
.\" Manpage for Holberton _Printf Project.
.\"
.TH man  "19 Nov 2023" "1.0" "_Printf Project Holberton man page"
.SH NAME
._Printf
\- The function produces output according to a format that the printf would.

.SH SYNOPSIS
#include "Main.h"

int _printf(const char *format, ...)

_printf[argument]...

.SH DESCRIPTION
The Printf function provided prints the numerical values inputted or the ASCII characters of the specifiers given the argument. The _printf in this repository includes our attempt of the  printf function and its basic features


Return: The number of characters that are printed (excluding the null byte used which ends the output of the string

Conversion specifiers: The different conversion specifiers that were tasked in this Project and explained below.

%c - print a single character.

%s - print a string.

%d - print a integer as a signed decimal (base 10) number.

%i - print same as "d", integer in base 10.

% - print a percent sign (%).

Below there are a few examples of how the conversion specifiers are inputted in the correct format to produce a output.

.SH For example:

_printf("Character: %c", character);

_printf("String: %s", string);

_printf("Integer: %d", integerdec);

_printf("Integer: %i", integer); 



.SH SEE ALSO
printf(1), _printf(2), printf(3), sprintf(), snprintf(), vsprintf(), vsnprintf().

.SH BUGS
If there is a bug, report to 7701@holbertonschool.com or NAHUFEOLA@gmail.com

.SH AUTHOR
Martin Stein and Nahuel Suarez- Students at Holberton School Uruguay.


